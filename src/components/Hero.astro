---

---

<section class="hero" id="about">
    <!-- Éléments décoratifs -->
    <div class="hero-bg-elements">
        <div class="hero-shape1"></div>
        <div class="hero-shape2"></div>
        <div class="hero-dots"></div>
    </div>

    <div class="hero-content">
        <article class="hero-information" data-aos="fade-up">
            <div class="hero-eyebrow">
                <span class="wave">👋</span> Bonjour, je suis
            </div>
            <h1 class="hero-presentation">
                Jean-José <span class="name">Muller</span>
            </h1>
            <h2 class="hero-title">
                Étudiant dans les métiers du multimédia et de l'internet
            </h2>
            <p class="hero-text">
                Actuellement en 2ème année, je suis passionné par le design et
                le développement web. Je suis à la recherche d'une alternance à la rentrée de septembre 2025 pour
                mettre en pratique mes compétences et apprendre auprès de
                professionnels.
            </p>

            <div class="hero-tags">
                <span class="hero-tag">HTML/CSS</span>
                <span class="hero-tag">JavaScript</span>
                <span class="hero-tag">Design UI/UX</span>
                <span class="hero-tag">Multimédia</span>
            </div><div class="CTAs">
                <a href="#projects" class="CTA">
                    <span>Voir mes projets</span>
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        width="20"
                        height="20"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                        stroke-linecap="round"
                        stroke-linejoin="round"
                    >
                        <path d="M5 12h14"></path>
                        <path d="M12 5l7 7-7 7"></path>
                    </svg>
                </a>
                <a
                    href="https://www.linkedin.com/in/jean-jos%C3%A9-muller-892b91276/"
                    target="_blank"
                    rel="noopener noreferrer"
                    class="CTA2"
                >
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        width="18"
                        height="18"
                        viewBox="0 0 24 24"
                        fill="currentColor"
                    >
                        <path
                            d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"
                        ></path>
                    </svg>
                    LinkedIn
                </a>
            </div>
        </article>
    </div>
    <div class="hero-image" data-aos="fade-up" data-aos-delay="200">
        <div class="image-wrapper">
            <img
                class="hero__img"
                src="/Muller_Photo.jpeg"
                alt="Portrait de Muller Jean-José"
                loading="eager"
                style="cursor: pointer;"
                onclick="openOverlay('/Muller_Photo.jpeg')"
            />
            <div class="hero-blob"></div>
            <div class="image-shine"></div>
        </div>
        <div class="hero-image-dots"></div>
    </div>
</section>

<div id="overlay" class="overlay" onclick="closeOverlay(event)">
    <span class="close" onclick="closeOverlay(event)">&times;</span>
    <img id="overlayImage" src="" alt="" />
</div>

<script>
    function openOverlay(imageSrc: string) {
        const overlay = document.getElementById('overlay');
        const overlayImage = document.getElementById('overlayImage') as HTMLImageElement | null;
        if (overlay && overlayImage) {
            overlayImage.src = imageSrc;
            overlay.style.display = 'flex';
        }
    }

    function closeOverlay(event: MouseEvent) {
        const overlay = document.getElementById('overlay');
        if (overlay) {
            const target = event.target as HTMLElement;
            if (target.id === 'overlay' || target.classList.contains('close')) {
                overlay.style.display = 'none';
            }
        }
    }
</script>

<style>
    .hero {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 3rem;
        padding: 8rem 3rem 6rem;
        max-width: var(--max-width);
        width: 100%;
        margin: 0 auto;
        position: relative;
        overflow: hidden;
    }

    .hero-bg-elements {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 1;
        overflow: hidden;
    }

    .hero-shape1 {
        position: absolute;
        width: 500px;
        height: 500px;
        background: linear-gradient(
            135deg,
            rgba(var(--blue-rgb-300), 0.3),
            rgba(var(--blue-rgb-500), 0.2)
        );
        border-radius: 40% 60% 70% 30% / 40% 50% 60% 50%;
        top: -200px;
        left: -200px;
        z-index: 1;
        animation: floating 20s infinite alternate ease-in-out;
    }

    .hero-shape2 {
        position: absolute;
        width: 300px;
        height: 300px;
        background: linear-gradient(
            135deg,
            rgba(var(--blue-rgb-400), 0.15),
            rgba(var(--blue-rgb-600), 0.1)
        );
        border-radius: 60% 40% 30% 70% / 50% 60% 30% 60%;
        bottom: -100px;
        right: -100px;
        z-index: 1;
        animation: floating 15s infinite alternate-reverse ease-in-out;
    }

    .hero-dots {
        position: absolute;
        width: 100%;
        height: 100%;
        background-image: radial-gradient(
            rgba(var(--blue-rgb-300), 0.3) 1px,
            transparent 1px
        );
        background-size: 30px 30px;
        opacity: 0.4;
        z-index: 1;
    }

    .hero-content {
        display: flex;
        flex-direction: column;
        justify-content: center;
        position: relative;
        z-index: 2;
        flex: 1;
    }

    .hero-eyebrow {
        font-size: 1.3rem;
        color: var(--blue-600);
        margin-bottom: 1.2rem;
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .hero-presentation {
        font-size: 4.2rem;
        font-weight: 800;
        margin-bottom: 1.5rem;
        line-height: 1.1;
        background: linear-gradient(to right, var(--blue-900), var(--blue-700));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        letter-spacing: -1px;
    }
    .hero-title {
        font-size: 1.5rem;
        margin-bottom: 1.8rem;
        color: var(--blue-700);
        font-weight: 500;
    }

    .name {
        background: linear-gradient(to right, var(--blue-600), var(--blue-500));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        position: relative;
        display: inline-block;
        z-index: 1;
    }

    .name::after {
        content: "";
        position: absolute;
        width: 100%;
        height: 10px;
        background: linear-gradient(
            90deg,
            rgba(var(--blue-rgb-200), 0.7),
            rgba(var(--blue-rgb-100), 0.4)
        );
        bottom: 10px;
        left: 0;
        z-index: -1;
        border-radius: 10px;
    }

    .hero-text {
        margin-bottom: 2.8rem;
        max-width: 520px;
        font-size: 1.15rem;
        color: var(--blue-700);
        line-height: 1.8;
    }

    .hero-tags {
        display: flex;
        flex-wrap: wrap;
        gap: 0.8rem;
        margin-bottom: 2.5rem;
    }

    .hero-tag {
        background-color: var(--blue-50);
        color: var(--blue-700);
        padding: 0.6rem 1.2rem;
        border-radius: 30px;
        font-size: 0.95rem;
        font-weight: 500;
        transition: all 0.3s ease;
        border: 1px solid var(--blue-200);
        box-shadow: 0 3px 10px rgba(var(--blue-rgb-500), 0.05);
    }

    .hero-tag:hover {
        background-color: var(--blue-100);
        transform: translateY(-3px);
        box-shadow: 0 5px 15px rgba(var(--blue-rgb-500), 0.1);
        border-color: var(--blue-300);
    }

    .CTAs {
        display: flex;
        gap: 1.5rem;
    }
    .CTA,
    .CTA2,
    .CTA3 {
        padding: 1rem 2rem;
        border-radius: 12px;
        text-decoration: none;
        font-weight: 600;
        transition: all 0.3s ease;
        font-size: 1rem;
        display: flex;
        align-items: center;
        gap: 0.7rem;
    }

    .CTA {
        background: linear-gradient(to right, var(--blue-600), var(--blue-500));
        color: white;
        box-shadow: 0 5px 20px rgba(var(--blue-rgb-500), 0.3);
        border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .CTA2,
    .CTA3 {
        border: 2px solid var(--blue-300);
        color: var(--blue-600);
        background: white;
    }

    .CTA svg {
        transition: transform 0.3s ease;
    }
    .CTA:hover {
        transform: translateY(-3px);
        box-shadow: 0 10px 25px rgba(var(--blue-rgb-500), 0.4);
        filter: brightness(1.05);
    }

    .CTA:hover svg {
        transform: translateX(4px);
    }

    .CTA2:hover,
    .CTA3:hover {
        background-color: var(--blue-50);
        transform: translateY(-3px);
        box-shadow: 0 8px 20px rgba(var(--blue-rgb-300), 0.15);
        color: var(--blue-700);
        border-color: var(--blue-400);
    }

    .wave {
        animation: wave 2.5s infinite;
        display: inline-block;
        transform-origin: 70% 70%;
        font-size: 1.4rem;
        margin-right: 0.3rem;
    }

    @keyframes wave {
        0% {
            transform: rotate(0deg);
        }
        10% {
            transform: rotate(14deg);
        }
        20% {
            transform: rotate(-8deg);
        }
        30% {
            transform: rotate(14deg);
        }
        40% {
            transform: rotate(-4deg);
        }
        50% {
            transform: rotate(10deg);
        }
        60% {
            transform: rotate(0deg);
        }
        100% {
            transform: rotate(0deg);
        }
    }

    @keyframes floating {
        0% {
            transform: translate(0, 0) rotate(0deg);
        }
        25% {
            transform: translate(10px, -15px) rotate(2deg);
        }
        50% {
            transform: translate(20px, 10px) rotate(-1deg);
        }
        75% {
            transform: translate(-5px, 15px) rotate(1deg);
        }
        100% {
            transform: translate(0, 0) rotate(0deg);
        }
    }

    .hero-image {
        position: relative;
        z-index: 2;
        padding: 1.5rem;
        width: 380px; /* Taille réduite */
        max-width: 100%;
    }

    .image-wrapper {
        position: relative;
        border-radius: 20px;
        overflow: hidden;
        box-shadow: 0 15px 35px rgba(var(--blue-rgb-500), 0.25);
        transition: all 0.5s ease;
    }

    .image-wrapper:hover {
        transform: translateY(-8px) rotate(2deg);
        box-shadow: 0 25px 40px rgba(var(--blue-rgb-500), 0.4);
    }

    .hero__img {
        display: block;
        width: 100%;
        height: auto;
        border-radius: 20px;
        transition: all 0.5s ease;
        transform-origin: center;
    }

    .image-wrapper:hover .hero__img {
        transform: scale(1.05);
    }

    .hero-blob {
        position: absolute;
        width: 110%;
        height: 110%;
        top: -5%;
        left: -5%;
        background: linear-gradient(
            45deg,
            rgba(var(--blue-rgb-400), 0.6),
            rgba(var(--blue-rgb-600), 0.4)
        );
        border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
        z-index: -1;
        animation: blobMovement 10s infinite alternate ease-in-out;
    }

    .image-shine {
        position: absolute;
        top: -50%;
        left: -50%;
        width: 200%;
        height: 200%;
        background: linear-gradient(
            45deg,
            rgba(255, 255, 255, 0) 0%,
            rgba(255, 255, 255, 0.8) 50%,
            rgba(255, 255, 255, 0) 100%
        );
        transform: rotate(25deg);
        animation: shineEffect 5s infinite;
        opacity: 0;
        pointer-events: none;
    }

    .image-wrapper:hover .image-shine {
        opacity: 1;
    }

    @keyframes shineEffect {
        0% {
            opacity: 0;
            transform: rotate(25deg) translateX(-100%);
        }
        20% {
            opacity: 0.8;
        }
        100% {
            opacity: 0;
            transform: rotate(25deg) translateX(100%);
        }
    }

    .overlay {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.8);
        justify-content: center;
        align-items: center;
        z-index: 9999;
    }

    .overlay img {
        max-width: 90%;
        max-height: 90%;
        border-radius: 10px;
    }

    .overlay .close {
        position: absolute;
        top: 20px;
        right: 20px;
        font-size: 2rem;
        color: white;
        cursor: pointer;
    }

    @media (max-width: 1200px) {
        .hero {
            padding: 7rem 2rem 5rem;
        }

        .hero-presentation {
            font-size: 3.8rem;
        }

        .experience-badge {
            right: 0;
            width: 80px;
            height: 80px;
        }

        .exp-years {
            font-size: 1.8rem;
        }
    }

    @media (max-width: 1100px) {
        .hero {
            flex-direction: column;
            text-align: center;
            padding-top: 6rem;
            gap: 2rem;
        }

        .hero-content {
            align-items: center;
        }

        .hero-text {
            max-width: 100%;
        }

        .CTAs {
            justify-content: center;
        }

        .hero-eyebrow {
            justify-content: center;
        }

        .name::after {
            bottom: 5px;
        }
    }

    @media (max-width: 992px) {
        .hero {
            padding: 6rem 2rem 4rem;
            gap: 2rem;
        }

        .hero-presentation {
            font-size: 3.2rem;
        }

        .hero-title {
            font-size: 1.3rem;
        }

        .hero-text {
            font-size: 1.05rem;
        }

        .hero__img {
            max-width: 380px;
        }
    }
    @media (max-width: 768px) {
        .hero {
            padding: 5rem 1.5rem 3rem;
        }

        .hero-presentation {
            font-size: 3rem;
        }

        .hero-title {
            font-size: 1.3rem;
        }

        .hero-text {
            font-size: 1rem;
        }

        .hero-tags {
            justify-content: center;
        }

        .CTAs {
            flex-direction: column;
            width: 100%;
            max-width: 280px;
        }

        .CTA,
        .CTA2,
        .CTA3 {
            width: 100%;
            justify-content: center;
        }
    }    @media (max-width: 576px) {
        .hero {
            padding: 5rem 1rem 3rem;
        }

        .hero-presentation {
            font-size: 2.5rem;
        }

        .hero-title {
            font-size: 1.1rem;
        }
        
        .hero-text {
            font-size: 0.95rem;
        }

        .hero-tags {
            justify-content: center;
            flex-wrap: wrap;
            gap: 0.5rem;
        }
        
        .hero-tag {
            font-size: 0.85rem;
            padding: 0.4rem 0.8rem;
        }

        .CTAs {
            flex-direction: column;
            gap: 1rem;
            width: 100%;
            max-width: 300px;
            margin: 0 auto;
        }

        .CTA,
        .CTA2,
        .CTA3 {
            width: 100%;
            justify-content: center;
            padding: 0.7rem 1rem;
            font-size: 0.95rem;
        }

        .hero__img {
            max-width: 300px;
            border-radius: 20px;
        }

        .experience-badge {
            width: 70px;
            height: 70px;
            right: 10px;
            bottom: 20px;
        }

        .exp-years {
            font-size: 1.6rem;
        }
        }

        .exp-text {
            font-size: 0.65rem;
        }
    }
</style>

