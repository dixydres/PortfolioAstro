---
import { getCollection } from "astro:content";
import Basic from "../layouts/Basic.astro";
import Hero from "../components/Hero.astro";
import Projects from "../components/Projects.astro";
import ProjectGrid from "../components/ProjectGrid.astro";
import Education from "../components/Education.astro";
import Contact from "../components/Contact.astro";

// Get all work entries
const allWorks = await getCollection("works");
---

<Basic title="Portfolio - Jean-José Muller">
	<div class="top">
		<Hero />
	</div>

	<Education />

	<div class="skills" data-aos="fade-up">
		<div class="skills-shapes">
			<div class="skill-shape shape1"></div>
			<div class="skill-shape shape2"></div>
			<div class="skill-dots"></div>
		</div>
		<div class="skills-container">
			<div class="skills-header">
				<h2 class="skills-title">Mes compétences</h2>
				<div class="skills-divider"></div>
				<p class="skills-subtitle">
					Découvrez mes domaines d'expertise et compétences techniques
				</p>
			</div>

			<div class="skills-grid">
				<div class="skill-card" data-aos="fade-up" data-aos-delay="100">
					<div class="skill-icon">
						<svg
							xmlns="http://www.w3.org/2000/svg"
							width="24"
							height="24"
							viewBox="0 0 24 24"
							fill="none"
							stroke="currentColor"
							stroke-width="2"
							stroke-linecap="round"
							stroke-linejoin="round"
							><path
								d="M2 16.1A5 5 0 0 1 5.9 20M2 12.05A9 9 0 0 1 9.95 20M2 8V6a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2h-6"
							></path><line x1="2" y1="20" x2="2" y2="20"
							></line></svg
						>
					</div>
					<h3>Développement Web</h3>
					<p>
						Création de sites web modernes et responsive avec HTML,
						CSS et JavaScript.
					</p>
					<div class="skill-tags">
						<span>HTML/CSS</span>
						<span>JavaScript</span>
						<span>Astro</span>
					</div>
				</div>

				<div class="skill-card" data-aos="fade-up" data-aos-delay="200">
					<div class="skill-icon">
						<svg
							xmlns="http://www.w3.org/2000/svg"
							width="24"
							height="24"
							viewBox="0 0 24 24"
							fill="none"
							stroke="currentColor"
							stroke-width="2"
							stroke-linecap="round"
							stroke-linejoin="round"
							><rect
								x="3"
								y="3"
								width="18"
								height="18"
								rx="2"
								ry="2"></rect><circle cx="8.5" cy="8.5" r="1.5"
							></circle><polyline points="21 15 16 10 5 21"
							></polyline></svg
						>
					</div>
					<h3>Design & UI/UX</h3>
					<p>
						Conception d'interfaces utilisateur et travail sur
						l'expérience utilisateur dans le cadre de mes études
						MMI.
					</p>
					<div class="skill-tags">
						<span>Figma</span>
						<span>Photoshop</span>
						<span>Illustrator</span>
					</div>
				</div>

				<div class="skill-card" data-aos="fade-up" data-aos-delay="300">
					<div class="skill-icon">
						<svg
							xmlns="http://www.w3.org/2000/svg"
							width="24"
							height="24"
							viewBox="0 0 24 24"
							fill="none"
							stroke="currentColor"
							stroke-width="2"
							stroke-linecap="round"
							stroke-linejoin="round"
							><path
								d="M15 6v12a3 3 0 1 0 3-3H6a3 3 0 1 0 3 3V6a3 3 0 1 0-3 3h12a3 3 0 1 0-3-3"
							></path></svg
						>
					</div>
					<h3>Compétences Multimédia</h3>
					<p>
						Création de contenu multimédia varié grâce à ma
						formation MMI et mes projets personnels.
					</p>
					<div class="skill-tags">
						<span>Montage Vidéo</span>
						<span>Photographie</span>
						<span>Animation</span>
					</div>
				</div>
			</div>
		</div>
	</div>

	<Projects showFilters={false}>
		<ProjectGrid count={3} />
	</Projects>

	<Contact />
</Basic>

<style>
	.top {
		background-color: var(--blue-50);
		min-height: 100vh;
		display: flex;
		flex-direction: column;
		position: relative;
		overflow: hidden;
	}

	.top::after {
		content: "";
		position: absolute;
		bottom: 0;
		left: 0;
		width: 100%;
		height: 70px;
		background: linear-gradient(
			to bottom right,
			transparent 49.5%,
			white 50%
		);
		z-index: 1;
	}

	.skills {
		padding: 7rem 3rem;
		background: linear-gradient(180deg, var(--blue-50), white);
		position: relative;
		overflow: hidden;
	}

	.skills-shapes {
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		z-index: 1;
	}

	.skill-shape {
		position: absolute;
		border-radius: 50%;
		opacity: 0.1;
	}

	.shape1 {
		width: 350px;
		height: 350px;
		background: var(--blue-600);
		top: -100px;
		left: -150px;
	}

	.shape2 {
		width: 250px;
		height: 250px;
		background: var(--blue-500);
		bottom: 10%;
		right: -70px;
	}

	.skill-dots {
		position: absolute;
		width: 100%;
		height: 100%;
		background-image: radial-gradient(var(--blue-300) 1px, transparent 1px);
		background-size: 25px 25px;
		opacity: 0.2;
	}

	.skills-container {
		max-width: var(--max-width);
		margin: 0 auto;
		position: relative;
		z-index: 2;
	}

	.skills-header {
		text-align: center;
		margin-bottom: 3.5rem;
	}

	.skills-title {
		font-size: 2.8rem;
		font-weight: 700;
		color: var(--blue-800);
		text-align: center;
		margin-bottom: 0.8rem;
	}

	.skills-subtitle {
		max-width: 600px;
		margin: 1.5rem auto 0;
		color: var(--blue-700);
		font-size: 1.1rem;
		line-height: 1.6;
	}

	.skills-divider {
		width: 80px;
		height: 4px;
		background: linear-gradient(90deg, var(--blue-600), var(--blue-400));
		margin: 0 auto;
		border-radius: 2px;
	}

	.skills-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
		gap: 2.5rem;
	}

	.skill-card {
		background: white;
		border-radius: 20px;
		padding: 2.5rem;
		box-shadow: 0 15px 35px rgba(var(--blue-rgb-500), 0.1);
		border: 2px solid var(--blue-100);
		transition: all 0.4s ease;
		position: relative;
		z-index: 1;
		overflow: hidden;
	}

	.skill-card::before {
		content: "";
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 5px;
		background: linear-gradient(90deg, var(--blue-600), var(--blue-400));
		opacity: 0;
		transition: opacity 0.3s ease;
		z-index: 2;
	}

	.skill-card:hover {
		transform: translateY(-10px);
		box-shadow: 0 25px 50px rgba(var(--blue-rgb-500), 0.15);
		border-color: var(--blue-300);
	}

	.skill-card:hover::before {
		opacity: 1;
	}

	.skill-icon {
		background: linear-gradient(135deg, var(--blue-100), var(--blue-50));
		width: 64px;
		height: 64px;
		border-radius: 16px;
		display: flex;
		align-items: center;
		justify-content: center;
		color: var(--blue-600);
		margin-bottom: 1.8rem;
		box-shadow: 0 5px 15px rgba(var(--blue-rgb-300), 0.1);
		border: 1px solid rgba(var(--blue-rgb-200), 0.3);
		transition: all 0.3s ease;
	}

	.skill-card:hover .skill-icon {
		transform: scale(1.1);
		color: var(--blue-700);
		background: linear-gradient(135deg, var(--blue-200), var(--blue-100));
	}

	.skill-card h3 {
		font-size: 1.4rem;
		font-weight: 600;
		margin-bottom: 1rem;
		color: var(--blue-800);
	}

	.skill-card p {
		color: var(--blue-700);
		margin-bottom: 1.8rem;
		line-height: 1.7;
	}

	.skill-tags {
		display: flex;
		flex-wrap: wrap;
		gap: 0.7rem;
	}

	.skill-tags span {
		background-color: var(--blue-50);
		color: var(--blue-700);
		padding: 0.5rem 1rem;
		border-radius: 10px;
		font-size: 0.9rem;
		font-weight: 500;
		transition: all 0.3s ease;
		border: 1px solid rgba(var(--blue-rgb-100), 0.3);
	}

	.skill-tags span:hover {
		background-color: var(--blue-100);
		transform: translateY(-3px);
		box-shadow: 0 5px 10px rgba(var(--blue-rgb-300), 0.1);
		border-color: var(--blue-200);
	}

	@media (max-width: 992px) {
		.skills {
			padding: 6rem 2rem;
		}

		.skills-grid {
			gap: 2rem;
		}
	}

	@media (max-width: 768px) {
		.skills {
			padding: 5rem 1.5rem;
		}

		.skills-title {
			font-size: 2.4rem;
		}

		.skills-subtitle {
			font-size: 1rem;
			padding: 0 1rem;
		}

		.skills-grid {
			grid-template-columns: 1fr;
			max-width: 450px;
			margin: 0 auto;
		}

		.skill-card {
			padding: 2rem;
		}
	}

	@media (max-width: 480px) {
		.skill-card {
			padding: 1.5rem;
		}

		.skill-icon {
			width: 50px;
			height: 50px;
			margin-bottom: 1.5rem;
		}

		.skill-card h3 {
			font-size: 1.3rem;
		}
	}
</style>
